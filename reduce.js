[1, 2, 3, 4].reduce((x, y) => console.log(x, y));

//1 2 and undefined 3 and undefined 4

// If we don't pass initial value, then by default x will be first value of array,
// and y will be second value of array.
